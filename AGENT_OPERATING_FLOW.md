# AGENT_OPERATING_FLOW.md - 育児グッズナビ運用フロー

育児グッズナビは、クロッピー本体を編集長/PMとして、専門エージェントがSlack上で見える形で分担・レビュー・実装を回す。

## 目的

- Kaoruが各エージェントのやりとりをSlackで追えるようにする。
- 役割ごとの判断を分け、記事改善・導線改善・安全確認を小さく継続する。
- 外部投稿、課金、広告、APIキー、破壊的操作などのリスクは必ずKaoru判断に戻す。

## 推奨Slackチャンネル構成

| チャンネル案 | 用途 | 投稿者 | Kaoru確認ポイント |
| --- | --- | --- | --- |
| `#育児ナビ-編集会議` | 全体方針、今日の着手、成果報告、判断待ち | クロッピー本体、全エージェント | 優先順位、公開判断、保留判断 |
| `#育児ナビ-調査設計` | 検索需要、競合、記事構成、内部リンク設計 | marketer, architect | どの記事群を伸ばすか |
| `#育児ナビ-実装レビュー` | 実装差分、レビュー、修正依頼、公開前確認 | coder, reviewer, refactorer, security | 実装の採否、安全確認 |

まずは3チャンネルで十分。増やしすぎると運用が重くなるため、専用チャンネル追加は必要になってから行う。

## エージェント別の主な発言場所

| Agent ID | 主チャンネル | 補助チャンネル | 役割 |
| --- | --- | --- | --- |
| `ikujinavi-marketer` | `#育児ナビ-調査設計` | `#育児ナビ-編集会議` | 検索需要、競合、PV改善仮説 |
| `ikujinavi-architect` | `#育児ナビ-調査設計` | `#育児ナビ-編集会議` | サイト構造、記事群、導線設計 |
| `ikujinavi-coder` | `#育児ナビ-実装レビュー` | `#育児ナビ-編集会議` | HTML/CSS/Markdown実装、ビルド確認 |
| `ikujinavi-reviewer` | `#育児ナビ-実装レビュー` | `#育児ナビ-編集会議` | 読者目線、検索意図、表現、リンク確認 |
| `ikujinavi-security` | `#育児ナビ-実装レビュー` | `#育児ナビ-編集会議` | 外部JS、秘密情報、公開設定、安全表現リスク |
| `ikujinavi-refactorer` | `#育児ナビ-実装レビュー` | `#育児ナビ-編集会議` | 重複整理、保守性改善 |
| クロッピー本体 | `#育児ナビ-編集会議` | 全チャンネル | PM/編集長、タスク起票、採否、報告 |

## 標準運用フロー

### 1. 起票

クロッピー本体が `#育児ナビ-編集会議` に投稿する。

```text
[育児グッズナビ] 今日の運用タスク
目的:
対象ページ/記事:
期待する成果:
制約:
担当順:
判断が必要な点:
```

### 2. 調査・設計

1. marketer が `#育児ナビ-調査設計` に検索意図・競合・仮説を投稿。
2. architect が同チャンネルで記事構造、内部リンク、改善単位を決める。
3. クロッピー本体が `#育児ナビ-編集会議` に採用方針を短く戻す。

### 3. 実装

1. coder が `#育児ナビ-実装レビュー` に実装方針を出す。
2. 小さく変更し、差分・確認結果を投稿。
3. 必要に応じて refactorer が保守性だけを小さく改善する。

### 4. レビュー

1. reviewer が読者目線・検索意図・内部リンク・断定表現を確認。
2. security は以下の条件に当てはまる場合だけ確認する。
   - 外部JSや計測タグを触る
   - 公開設定を触る
   - 秘密情報や環境変数に関係する
   - 医療・安全・発達・事故防止に近い表現がある

### 5. 公開・報告

クロッピー本体が最終確認し、許可済み範囲なら commit/push する。
その後 `#育児ナビ-編集会議` に投稿する。

```text
[育児グッズナビ] 完了報告
変更内容:
確認したこと:
commit:
残課題:
次にKaoruが判断すること:
```

## 判断待ちルール

以下は必ずKaoruに戻す。

- 外部投稿、SNS投稿、メール送信
- 課金、広告、購入、サブスク
- APIキー、認証情報、秘密情報、計測IDの設定
- GitHub Pages / DNS / CNAME / repo設定変更
- force push、履歴改変、大量削除
- 医療・安全・発達・事故防止・リコール・価格・在庫について断定が必要な表現

## Slackでの見え方ルール

- 1投稿1論点にする。
- 長い調査は要点、根拠、次アクションだけを書く。
- Kaoruへの判断依頼は `#育児ナビ-編集会議` に集約する。
- 調査ログやレビュー議論は各専門チャンネルに残す。
- Madobeの話題は混ぜない。

## 最小開始案

1. Slackで以下3チャンネルを作る。
   - `#育児ナビ-編集会議`
   - `#育児ナビ-調査設計`
   - `#育児ナビ-実装レビュー`
2. クロッピー本体と6つの育児ナビagentを参加させる。
3. 最初の1週間は毎日1タスクだけ流す。
4. うまく回ったら、PVチェックや週次棚卸しを追加する。
